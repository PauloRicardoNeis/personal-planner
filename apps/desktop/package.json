{
  "name": "desktop",
  "private": true,
  "version": "0.1.13",
  "scripts": {
    "build": "tauri build",
    "build:release": "tauri build --config src-tauri/tauri.conf.release.json",
    "dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
